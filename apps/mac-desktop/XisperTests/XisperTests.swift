import Testing
@testable import Xisper

/// Placeholder test suite — will be expanded as each phase adds testable components.
struct XisperTests {

    @Test func appLaunches() async throws {
        // Smoke test: verify the app bundle can be imported without crashing.
        // Real tests will cover HotkeySystem, RecordingCoordinator, ASRClient, etc.
        #expect(true)
    }
}
