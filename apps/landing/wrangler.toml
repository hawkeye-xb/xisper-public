# Cloudflare Pages configuration for landing page
# Deploy: wrangler pages deploy dist --project-name xisper-landing

name = "xisper-landing"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"
